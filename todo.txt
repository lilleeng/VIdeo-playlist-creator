Short term:
-Add code for deleting temporary files
-Make pydub work by installing 'ffmpeg'
-Segment the code into functions
-For single long video, print timestamps.

Long term:
-Retain audio bitrate in the video created.
-Create function for concatenating video clips


Git commands to add all except:
git add .
git reset -- [FILE NAME]
git commit -m "[MESSAGE]"
git push